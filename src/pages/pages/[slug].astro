---
const { slug } = Astro.params;
const search = Astro.url.search || '';

// Route /pages/home (or /pages/slug from EmDash CMS) to the canonical route
if (slug === 'home' || !slug) {
	return Astro.redirect(`/${search}`, 302);
}

return Astro.redirect(`/${slug}${search}`, 302);
---
